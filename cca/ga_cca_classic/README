
#
# Environment Variables. Others are taken from $CCAFE_HOME/Makefile.Vars 
# (Makefile.Vars is included in Makefile.Rules)
#
export CCAFE_HOME=/msrc/proj/cca/manoj/dccafe/cxx
export GA_HOME=/msrc/home/manoj/GA
export LIBMPI=-lmpich

######################################################################
# In IA/64:
#  Make sure all the codes are compiled using -fpic option 
# (i.e. the GA API  codes too). As any code that is linked to a .so 
# in IA64 should be compiled with -fpic option. In GA-Classic we link
# all the ga libraries to the GAServices.so

#For Linux64-Quadrics only. Compile GA-Classic wih this settings
export LINUX64_QUADRICS=y

# In IA-64, Compile GA libraries with GA_C_CORE set
export GA_C_CORE=y 
#######################################################################
